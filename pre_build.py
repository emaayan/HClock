# Import the current working construction
# environment to the `env` variable.
# alias of `env = DefaultEnvironment()`
Import ("env")
import os

import shutil

dst="./lib/libzmanim"
# shutil.copytree("../libzmanim/include", dst,dirs_exist_ok=True)
# shutil.copytree("../libzmanim/src", dst,dirs_exist_ok=True)
# shutil.copytree("../libzmanim/windows", dst,dirs_exist_ok=True)
